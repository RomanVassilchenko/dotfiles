# Config with local state (project trust levels, model migrations)
config.toml

# Runtime & state
auth.json
history.jsonl
logs_*.sqlite
logs_*.sqlite-shm
logs_*.sqlite-wal
state_*.sqlite
state_*.sqlite-shm
state_*.sqlite-wal
models_cache.json
version.json
.personality_migration

# Runtime dirs
log/
memories/
sessions/
shell_snapshots/
tmp/

# Rules may contain secrets (passwords, etc.)
rules/

# System-managed skills
skills/
